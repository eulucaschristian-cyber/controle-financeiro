[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["COREPACK_INTEGRITY_KEYS=\"\" corepack enable && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "pnpm run start"
